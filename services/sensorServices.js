import {
  accelerometer,
  gyroscope,
  magnetometer,
  setUpdateIntervalForType,
  SensorTypes,
} from 'react-native-sensors';

export const recordIMUData = () => {
  //   accelerometer.subscribe(({x, y, z}) => {
  //     console.log(`You moved your phone with ${x + y + z}`);
  //   });
};
